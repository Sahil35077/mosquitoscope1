flask>=3.0
gunicorn>=22.0
huggingface_hub>=0.23
requests>=2.31
timm>=0.9
Pillow>=10.0
# CPU-only PyTorch (smaller install for Render). Install torch separately in Dockerfile/build.
torch>=2.0
torchvision>=0.15
